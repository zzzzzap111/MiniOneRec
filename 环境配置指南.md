# MiniOneRec 环境配置指南

## 📋 目录

1. [系统要求](#系统要求)
2. [快速开始](#快速开始)
3. [详细配置步骤](#详细配置步骤)
4. [CUDA/GPU 配置](#cudagpu-配置)
5. [可选依赖安装](#可选依赖安装)
6. [环境验证](#环境验证)
7. [常见问题排查](#常见问题排查)

---

## 系统要求

### 硬件要求

| 组件 | 最低要求 | 推荐配置 |
|------|---------|---------|
| **GPU** | 1× RTX 3090 (24GB) | 4-8× A100/H100 (80GB) |
| **显存** | 16GB | 80GB+ |
| **内存** | 32GB | 64GB+ |
| **存储** | 100GB 可用空间 | 500GB+ SSD |

### 软件要求

| 软件 | 版本要求 | 说明 |
|------|---------|------|
| **Python** | 3.10+ | 推荐 3.11 |
| **CUDA** | 11.8+ | 用于 GPU 加速 |
| **cuDNN** | 8.0+ | CUDA 深度学习库 |
| **操作系统** | Linux / macOS / Windows | 推荐 Linux |

---

## 快速开始

### 方法一：使用 Conda（推荐）

```bash
# 1. 创建虚拟环境
conda create -n MiniOneRec python=3.11 -y
conda activate MiniOneRec

# 2. 安装依赖
pip install -r requirements.txt

# 3. 验证安装
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available())"
```

### 方法二：使用 venv

```bash
# 1. 创建虚拟环境
python3.11 -m venv venv
source venv/bin/activate  # Linux/macOS
# 或
venv\Scripts\activate  # Windows

# 2. 安装依赖
pip install -r requirements.txt

# 3. 验证安装
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available())"
```

---

## 详细配置步骤

### 步骤 1：安装 Python

#### Linux/macOS

```bash
# 使用 conda（推荐）
conda install python=3.11

# 或使用系统包管理器
# Ubuntu/Debian
sudo apt-get install python3.11 python3.11-venv

# macOS (使用 Homebrew)
brew install python@3.11
```

#### Windows

1. 从 [Python 官网](https://www.python.org/downloads/) 下载 Python 3.11
2. 安装时勾选 "Add Python to PATH"
3. 验证安装：
```bash
python --version
```

### 步骤 2：创建虚拟环境

```bash
# 使用 conda（推荐）
conda create -n MiniOneRec python=3.11 -y
conda activate MiniOneRec

# 或使用 venv
python3.11 -m venv MiniOneRec_env
source MiniOneRec_env/bin/activate  # Linux/macOS
MiniOneRec_env\Scripts\activate  # Windows
```

### 步骤 3：安装 PyTorch

**重要**：根据你的 CUDA 版本选择对应的 PyTorch 版本。

#### 检查 CUDA 版本

```bash
nvidia-smi  # 查看 CUDA 版本
# 或
nvcc --version
```

#### 安装 PyTorch

**CUDA 11.8（推荐，与 requirements.txt 匹配）**：

```bash
pip install torch==2.6.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

**CUDA 12.1**：

```bash
pip install torch==2.6.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

**CPU 版本（不推荐，仅用于测试）**：

```bash
pip install torch==2.6.0 torchvision torchaudio
```

### 步骤 4：安装项目依赖

```bash
# 进入项目目录
cd MiniOneRec

# 安装所有依赖
pip install -r requirements.txt
```

**注意**：`requirements.txt` 中已经包含了 PyTorch 的安装源，但建议先单独安装 PyTorch（步骤 3），然后再安装其他依赖。

### 步骤 5：安装可选依赖

根据你的使用场景，可能需要安装以下可选依赖：

#### 1. FAISS（用于 RQ-Kmeans）

```bash
# GPU 版本（推荐）
conda install -c conda-forge faiss-gpu

# CPU 版本
conda install -c conda-forge faiss-cpu
```

#### 2. k-means-constrained（用于 Constrained RQ-Kmeans）

```bash
pip install k-means-constrained
pip install polars
```

#### 3. PEFT（用于 LoRA/QLoRA，已在 requirements.txt 中）

```bash
pip install peft>=0.6.0
```

---

## CUDA/GPU 配置

### 检查 GPU 可用性

```bash
# 检查 NVIDIA 驱动
nvidia-smi

# 检查 PyTorch GPU 支持
python -c "import torch; print(f'CUDA available: {torch.cuda.is_available()}'); print(f'CUDA version: {torch.version.cuda}'); print(f'GPU count: {torch.cuda.device_count()}')"
```

### 配置 CUDA 环境变量（可选）

```bash
# 在 ~/.bashrc 或 ~/.zshrc 中添加
export CUDA_HOME=/usr/local/cuda
export PATH=$CUDA_HOME/bin:$PATH
export LD_LIBRARY_PATH=$CUDA_HOME/lib64:$LD_LIBRARY_PATH
```

### 多 GPU 配置

如果使用多 GPU 训练，需要配置 DeepSpeed 或 Accelerate：

```bash
# 配置 Accelerate
accelerate config

# 或使用 DeepSpeed 配置文件
# 项目已包含 config/zero2_opt.yaml
```

---

## 可选依赖安装

### 1. WandB（用于实验跟踪）

```bash
# 已在 requirements.txt 中，但需要登录
pip install wandb
wandb login
```

### 2. HuggingFace Hub（用于下载模型）

```bash
# 已在 requirements.txt 中，但需要登录（可选）
pip install huggingface-hub
huggingface-cli login
```

### 3. 其他工具

```bash
# Jupyter（用于交互式开发）
pip install jupyter ipykernel

# 代码格式化
pip install black isort

# 类型检查
pip install mypy
```

---

## 环境验证

### 完整验证脚本

创建 `check_env.py`：

```python
#!/usr/bin/env python
"""环境验证脚本"""

import sys

def check_python():
    """检查 Python 版本"""
    version = sys.version_info
    if version.major == 3 and version.minor >= 10:
        print(f"✅ Python {version.major}.{version.minor}.{version.micro}")
        return True
    else:
        print(f"❌ Python {version.major}.{version.minor}.{version.micro} (需要 3.10+)")
        return False

def check_torch():
    """检查 PyTorch"""
    try:
        import torch
        print(f"✅ PyTorch {torch.__version__}")
        
        if torch.cuda.is_available():
            print(f"✅ CUDA {torch.version.cuda} 可用")
            print(f"✅ GPU 数量: {torch.cuda.device_count()}")
            for i in range(torch.cuda.device_count()):
                print(f"   - GPU {i}: {torch.cuda.get_device_name(i)}")
        else:
            print("⚠️  CUDA 不可用（将使用 CPU，不推荐）")
        return True
    except ImportError:
        print("❌ PyTorch 未安装")
        return False

def check_dependencies():
    """检查关键依赖"""
    deps = [
        ("transformers", "Transformers"),
        ("accelerate", "Accelerate"),
        ("datasets", "Datasets"),
        ("peft", "PEFT"),
        ("bitsandbytes", "BitsAndBytes"),
        ("deepspeed", "DeepSpeed"),
        ("trl", "TRL"),
    ]
    
    all_ok = True
    for module, name in deps:
        try:
            mod = __import__(module)
            version = getattr(mod, "__version__", "unknown")
            print(f"✅ {name}: {version}")
        except ImportError:
            print(f"❌ {name} 未安装")
            all_ok = False
    
    return all_ok

def check_optional():
    """检查可选依赖"""
    optionals = [
        ("faiss", "FAISS"),
        ("wandb", "WandB"),
    ]
    
    for module, name in optionals:
        try:
            mod = __import__(module)
            version = getattr(mod, "__version__", "unknown")
            print(f"✅ {name}: {version} (可选)")
        except ImportError:
            print(f"⚠️  {name} 未安装 (可选)")

if __name__ == "__main__":
    print("=" * 50)
    print("MiniOneRec 环境检查")
    print("=" * 50)
    print()
    
    print("1. Python 版本检查")
    print("-" * 50)
    py_ok = check_python()
    print()
    
    print("2. PyTorch 检查")
    print("-" * 50)
    torch_ok = check_torch()
    print()
    
    print("3. 关键依赖检查")
    print("-" * 50)
    deps_ok = check_dependencies()
    print()
    
    print("4. 可选依赖检查")
    print("-" * 50)
    check_optional()
    print()
    
    print("=" * 50)
    if py_ok and torch_ok and deps_ok:
        print("✅ 环境配置完成！")
    else:
        print("❌ 环境配置不完整，请检查上述错误")
    print("=" * 50)
```

运行验证：

```bash
python check_env.py
```

---

## 常见问题排查

### 问题 1：CUDA 不可用

**症状**：
```python
torch.cuda.is_available()  # 返回 False
```

**解决方案**：

1. **检查 NVIDIA 驱动**：
```bash
nvidia-smi
```

2. **检查 CUDA 版本匹配**：
   - 确保 PyTorch 的 CUDA 版本与系统 CUDA 版本兼容
   - 如果使用 CUDA 11.8，安装 `cu118` 版本的 PyTorch

3. **重新安装 PyTorch**：
```bash
pip uninstall torch torchvision torchaudio
pip install torch==2.6.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

### 问题 2：bitsandbytes 安装失败

**症状**：
```
ERROR: Could not build wheels for bitsandbytes
```

**解决方案**：

1. **确保已安装 CUDA 工具包**：
```bash
nvcc --version
```

2. **使用预编译版本**：
```bash
pip install bitsandbytes==0.48.1
```

3. **如果仍然失败，尝试从源码安装**：
```bash
pip install git+https://github.com/TimDettmers/bitsandbytes.git
```

### 问题 3：FAISS 安装失败

**症状**：
```
ERROR: Could not find a version that satisfies the requirement faiss-gpu
```

**解决方案**：

1. **使用 conda 安装（推荐）**：
```bash
conda install -c conda-forge faiss-gpu
```

2. **或使用 pip**：
```bash
pip install faiss-gpu
```

### 问题 4：内存不足（OOM）

**症状**：
```
RuntimeError: CUDA out of memory
```

**解决方案**：

1. **减小 batch size**：
   - 在 `sft.sh` 或 `rl.sh` 中减小 `--batch_size` 和 `--micro_batch_size`

2. **使用梯度累积**：
   - 增加 `--gradient_accumulation_steps`

3. **使用 DeepSpeed ZeRO**：
```bash
accelerate launch --config_file ./config/zero2_opt.yaml sft.py ...
```

4. **使用 LoRA/QLoRA**：
   - 参考 `LoRA_QLoRA_使用指南.md`

### 问题 5：依赖版本冲突

**症状**：
```
ERROR: Cannot install package X because it conflicts with package Y
```

**解决方案**：

1. **使用新的虚拟环境**：
```bash
conda create -n MiniOneRec_new python=3.11 -y
conda activate MiniOneRec_new
pip install -r requirements.txt
```

2. **逐个安装依赖**：
```bash
# 先安装核心依赖
pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu118
pip install transformers==4.57.1
pip install accelerate==1.10.1
# ... 其他依赖
```

### 问题 6：加速库编译失败

**症状**：
```
ERROR: Failed building wheel for triton
```

**解决方案**：

1. **使用预编译版本**：
   - 确保使用与 PyTorch 匹配的 triton 版本（已在 requirements.txt 中）

2. **跳过编译，使用预编译包**：
```bash
pip install --prefer-binary triton
```

### 问题 7：多 GPU 训练问题

**症状**：
```
RuntimeError: NCCL error
```

**解决方案**：

1. **检查 NCCL 版本**：
```bash
python -c "import torch; print(torch.cuda.nccl.version())"
```

2. **设置环境变量**：
```bash
export NCCL_DEBUG=INFO
export NCCL_IB_DISABLE=1  # 如果使用 InfiniBand
```

3. **使用单 GPU 测试**：
```bash
CUDA_VISIBLE_DEVICES=0 python sft.py ...
```

---

## 环境配置检查清单

在开始训练前，请确认：

- [ ] Python 3.10+ 已安装
- [ ] 虚拟环境已创建并激活
- [ ] PyTorch 已安装且 CUDA 可用
- [ ] 所有依赖已安装（`pip install -r requirements.txt`）
- [ ] GPU 可用（`torch.cuda.is_available()` 返回 `True`）
- [ ] 环境验证脚本通过（`python check_env.py`）
- [ ] 可选依赖已安装（如 FAISS、k-means-constrained）
- [ ] WandB 已登录（如果使用）
- [ ] HuggingFace 已登录（如果需要下载模型）

---

## 快速安装脚本

创建 `setup_env.sh`：

```bash
#!/bin/bash
# MiniOneRec 环境快速安装脚本

set -e

echo "=========================================="
echo "MiniOneRec 环境安装脚本"
echo "=========================================="
echo

# 检查 conda
if command -v conda &> /dev/null; then
    echo "✅ 检测到 Conda"
    ENV_NAME="MiniOneRec"
    
    # 创建环境
    echo "创建 Conda 环境: $ENV_NAME"
    conda create -n $ENV_NAME python=3.11 -y
    
    # 激活环境
    echo "激活环境..."
    source $(conda info --base)/etc/profile.d/conda.sh
    conda activate $ENV_NAME
else
    echo "⚠️  未检测到 Conda，使用 venv"
    python3.11 -m venv MiniOneRec_env
    source MiniOneRec_env/bin/activate
fi

# 安装 PyTorch
echo
echo "安装 PyTorch..."
pip install torch==2.6.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# 安装依赖
echo
echo "安装项目依赖..."
pip install -r requirements.txt

# 安装可选依赖
echo
echo "安装可选依赖..."
conda install -c conda-forge faiss-gpu -y || echo "⚠️  FAISS 安装失败，可稍后手动安装"
pip install k-means-constrained polars || echo "⚠️  可选依赖安装失败"

# 验证
echo
echo "验证安装..."
python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA available: {torch.cuda.is_available()}')"

echo
echo "=========================================="
echo "✅ 环境安装完成！"
echo "=========================================="
echo "激活环境: conda activate $ENV_NAME"
echo "或: source MiniOneRec_env/bin/activate"
```

使用：

```bash
chmod +x setup_env.sh
./setup_env.sh
```

---

## 总结

### 推荐配置流程

1. **使用 Conda 创建环境**（最简单）
2. **先安装 PyTorch**（确保 CUDA 支持）
3. **再安装其他依赖**（`pip install -r requirements.txt`）
4. **验证环境**（运行 `check_env.py`）
5. **安装可选依赖**（根据需求）

### 关键要点

- ✅ **Python 3.11** 是最佳选择
- ✅ **CUDA 11.8** 与 requirements.txt 匹配
- ✅ **先安装 PyTorch**，再安装其他依赖
- ✅ **使用虚拟环境**，避免依赖冲突
- ✅ **验证 GPU 可用性**，确保训练能使用 GPU

### 获取帮助

如果遇到问题：

1. 查看本文档的 [常见问题排查](#常见问题排查) 部分
2. 检查项目的 [GitHub Issues](https://github.com/AkaliKong/MiniOneRec/issues)
3. 运行环境验证脚本：`python check_env.py`

---

**祝训练顺利！** 🚀

